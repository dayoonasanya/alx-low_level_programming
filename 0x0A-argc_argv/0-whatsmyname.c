#include <stdio.h>
/**
* main - function: prints given program name.
*@argc: No. of arguments.
*@argv: arguments array.
*Return: return zero (0).
*/
int main(int _attribute((__unused_)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
